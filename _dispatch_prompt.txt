## Task: Add reliability layer to LinkedIn Easy Apply bot

**Project:** C:\Projects\Mater-Browser

## Overview
Add 7 reliability improvements. Each is independent — implement all.

## 1. Validation Error Recovery (`src/agent/linkedin_form_filler.py`)

Add method to LinkedInFormFiller:
```python
def check_and_fix_errors(self) -> bool:
    """Check for validation errors and attempt to fix them. Returns True if errors found."""
```

Look for `.artdeco-inline-feedback--error`, `.artdeco-inline-feedback__message`, `[data-test-form-element-error-message]`, `.fb-form-element-error-text`. For each error, find the associated input (traverse up to form section, find the input/select/textarea within), fill with fallback. Log errors and fixes.

Then in `src/agent/linkedin_flow.py` `_process_easy_apply`, after `click_next()` succeeds, add error check:
```python
self._page.wait(500)
if filler.check_and_fix_errors():
    logger.info("Fixed validation errors, retrying next")
    filler.click_next()
    self._page.wait(500)
```

## 2. Resume Upload Handling (`src/agent/linkedin_form_filler.py`)

Add method:
```python
def _handle_resume_upload(self, modal: Locator) -> None:
    """Handle resume upload - select existing resume if available."""
```

1. Check if resume already selected (look for selected card state, checkmark)
2. If not, click first available resume card: `[data-test-document-upload-card]`, `.jobs-document-upload-redesign-card`, `button[aria-label*="resume" i]`
3. Also try "Choose Resume": `button[aria-label="Choose Resume"]`, `button:has-text("Choose")`
4. Don't block if no upload needed — just log and move on

Call from `fill_current_modal` BEFORE other fields.

## 3. Typeahead/Autocomplete Fields (`src/agent/linkedin_form_filler.py`)

Add method:
```python
def _is_typeahead_field(self, inp: Locator) -> bool:
    """Check if input is a typeahead/autocomplete field."""
```

Check: parent has "typeahead" or "text-entity-list" in class, `role="combobox"`, `aria-autocomplete="list"`, parent has `[data-test-text-entity-list-form-component]`.

Update `_fill_single_text_input`: if `_is_typeahead_field(inp)` is True, use autocomplete fill (type slowly, wait for suggestions, click first or Enter). Rename `_fill_autocomplete_location` to `_fill_autocomplete_field`.

## 4. Clean State Before Each Job (`src/agent/linkedin_flow.py`)

Add method:
```python
def _ensure_clean_state(self) -> None:
    """Dismiss any open modals/dialogs before starting new application."""
```

Check for visible `[role="dialog"]`, `.artdeco-modal`. If found, dismiss. Press Escape as fallback. Wait 500ms. Call at START of `apply()` before navigation.

## 5. Queue Re-validation (`src/automation/runner.py`)

Read `_apply_to_job` — it already calls `self._scorer.check_filter(job)`. Verify this works correctly. If already there, no changes. Just verify.

## 6. Randomized Delays (`src/automation/runner.py`)

Replace fixed delays with randomized:
```python
APPLY_DELAY_MIN_SECONDS: float = 3.0
APPLY_DELAY_MAX_SECONDS: float = 8.0
CYCLE_COOLDOWN_MIN_SECONDS: float = 5.0
CYCLE_COOLDOWN_MAX_SECONDS: float = 15.0
```

Use `random.uniform(min, max)` for both `time.sleep` calls in `_run_apply_cycle` and `_run_loop`.

Import `random` at top.

## 7. Better Stuck Recovery (`src/agent/linkedin_flow.py`)

In `_process_easy_apply`, when `stuck_count >= max_stuck`, before breaking, try ONE recovery:
1. Call `filler.check_and_fix_errors()` 
2. Scroll modal content down: `modal.evaluate("el => el.scrollTop += 300")`
3. Re-fill modal: `filler.fill_current_modal()`
4. Try `filler.click_next()` 
5. If click_next succeeds, reset stuck_count and continue
6. If fails, THEN abort and close modal

## Code Standards
- Static typing, logging module, guard clauses, functions under 25 lines, f-strings
- Import `random` where needed

## Verification
```
cd C:\Projects\Mater-Browser
python -c "from src.agent.linkedin_flow import LinkedInFlow; from src.agent.linkedin_form_filler import LinkedInFormFiller; from src.agent.page_classifier import PageClassifier; from src.automation.runner import AutomationRunner; print('OK')"
```


## Code Standards

Write code that looks inevitable. Follow these constraints:

**Restraint**
- Solve it in one file if possible
- No abstractions until the third time you need them
- No classes if functions will do
- No inheritance - use composition

**Functions**
- Max 25 lines, aim for 15
- One level of nesting max
- Name describes exactly what it does: `extract_billable_hours()` not `process_data()`
- Input → transform → output. No side effects unless that's the point.

**Files**
- Max 200 lines for new files
- One clear responsibility
- If you're adding a second "system" to a file, stop and split

**No Ceremony**
- No AbstractFactory, no IServiceProvider, no Manager classes
- No code "just in case" - solve the actual problem
- Delete commented-out code, don't keep it

**Data**
- Use dataclasses or plain dicts, not classes with only __init__ and getters
- Data flows obviously - reader should predict what happens next
- No global state

The best code is code you delete. Every line is a liability.
