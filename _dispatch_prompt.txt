Fix ATS enum mismatch - SMARTRECRUITERS and TALEO are missing from ATSType enum but used in registry.

## File: src/ats/detector.py

Add SMARTRECRUITERS and TALEO to the ATSType enum:

```python
class ATSType(Enum):
    """Known ATS systems."""
    WORKDAY = "workday"
    GREENHOUSE = "greenhouse"
    LEVER = "lever"
    ICIMS = "icims"
    PHENOM = "phenom"
    SMARTRECRUITERS = "smartrecruiters"  # ADD THIS
    TALEO = "taleo"  # ADD THIS
    INDEED = "indeed"
    LINKEDIN = "linkedin"
    UNKNOWN = "unknown"
```

Add URL patterns for SMARTRECRUITERS and TALEO in ATS_URL_PATTERNS:

```python
ATSType.SMARTRECRUITERS: [
    r"jobs\.smartrecruiters\.com",
    r"smartrecruiters\.com/.*jobs",
],
ATSType.TALEO: [
    r"taleo\.net",
    r"\.taleo\.net/careersection",
    r"oracle.*taleo",
],
```

Add page signatures for them in ATS_PAGE_SIGNATURES:

```python
ATSType.SMARTRECRUITERS: [
    "[class*='smartrecruiters']",
    ".sr-",
    "[data-sr]",
],
ATSType.TALEO: [
    "[class*='taleo']",
    ".taleo-",
    "#taleo",
],
```

## Verification
After editing, run:
```
python -c "from src.ats import ATSType, get_handler; print('OK')"
```

Write clean, scalable, modular, efficient code. Follow single responsibility principle. Do not repeat yourself. Use consistent naming conventions. No unnecessary comments.