Fix aggressive timeouts and add retry logic for navigation failures.

## Problem
- `EXTERNAL_REDIRECT_TIMEOUT_MS = 15000` is too short - Workday/Taleo can take 30+ seconds
- No retry on navigation failure - network hiccup = wrong page state
- Magic numbers scattered everywhere instead of constants

## Files to modify
- `src/agent/models.py`
- `src/agent/linkedin_flow.py`
- `src/agent/external_flow.py`
- `src/browser/page.py`

## models.py changes

Add/update these constants at top of file:
```python
# Timeouts
APPLICATION_TIMEOUT_SECONDS: float = 300.0  # 5 minutes total (was 180)
EXTERNAL_REDIRECT_TIMEOUT_MS: int = 30000   # 30 seconds (was 15000)
PAGE_LOAD_TIMEOUT_MS: int = 30000           # 30 seconds for page load
ELEMENT_TIMEOUT_MS: int = 10000             # 10 seconds for element wait
CLICK_TIMEOUT_MS: int = 8000                # 8 seconds for click
SHORT_WAIT_MS: int = 500                    # Short pause
MEDIUM_WAIT_MS: int = 1500                  # Medium pause  
LONG_WAIT_MS: int = 2500                    # Long pause

# Retry counts
MAX_NAVIGATION_RETRIES: int = 3
MAX_CLICK_RETRIES: int = 3
MAX_POPUP_WAIT_ATTEMPTS: int = 5
```

## page.py changes

1. Add retry logic to `goto()`:
```python
def goto(self, url: str, wait_until: str = "domcontentloaded", max_retries: int = 3) -> bool:
    """Navigate to URL with retry logic.
    
    Returns True if navigation succeeded, False if all retries failed.
    """
    from .models import MEDIUM_WAIT_MS
    
    for attempt in range(max_retries):
        try:
            logger.info(f"Navigating to: {url} (attempt {attempt + 1})")
            self._page.goto(url, wait_until=wait_until, timeout=30000)
            return True
        except Exception as e:
            error_msg = str(e).lower()
            if "err_aborted" in error_msg or "aborted" in error_msg:
                logger.warning(f"Navigation aborted (attempt {attempt + 1}): {e}")
                self.wait(MEDIUM_WAIT_MS)
                # Check if we're actually on the right page
                if url.split('?')[0] in self._page.url:
                    logger.info("Navigation succeeded despite abort")
                    return True
            elif attempt < max_retries - 1:
                logger.warning(f"Navigation failed (attempt {attempt + 1}): {e}")
                self.wait(MEDIUM_WAIT_MS)
            else:
                logger.error(f"Navigation failed after {max_retries} attempts: {e}")
                raise
    return False
```

## linkedin_flow.py changes

1. Import the new constants at top:
```python
from .models import (
    ...,
    MEDIUM_WAIT_MS, LONG_WAIT_MS, PAGE_LOAD_TIMEOUT_MS
)
```

2. Replace magic numbers:
- `self._page.wait(2000)` → `self._page.wait(LONG_WAIT_MS)`
- `self._page.wait(1500)` → `self._page.wait(MEDIUM_WAIT_MS)`
- `timeout=5000` → `timeout=PAGE_LOAD_TIMEOUT_MS`

## external_flow.py changes

1. Import the new constants
2. Replace all magic number waits with constants
3. Update `_wait_for_redirect()` to use `EXTERNAL_REDIRECT_TIMEOUT_MS` constant (it already does, but verify)

## Code standards
- Static typing everywhere
- Max 25 lines per function  
- Use logging module only

Write clean, scalable, modular, efficient code. Follow single responsibility principle. Do not repeat yourself. Use consistent naming conventions. No unnecessary comments.