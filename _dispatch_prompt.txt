Fix linkedin_form_filler.py to handle unfilled dropdowns and multi-select checkboxes.

PROBLEM: Bot gets stuck because:
1. Dropdowns with no answer stay at "Select an option" 
2. Multi-select checkbox groups (Select all that apply) don't get filled
3. Salary/numeric fields get "See resume" but need numbers

FIXES NEEDED:

1. In `_fill_single_select()` - Add fallback to select first non-empty option:
```python
if answer is None:
    # Fallback: select first non-placeholder option
    try:
        options = select.locator("option").all()
        for opt in options:
            val = opt.get_attribute("value")
            text = opt.text_content()
            # Skip placeholder options
            if val and val.strip() and text and "select" not in text.lower():
                select.select_option(value=val, timeout=3000)
                logger.info(f"Fallback select (first option): {question[:50]} = {text}")
                return True
    except Exception as e:
        logger.warning(f"Fallback select failed: {e}")
    return True
```

2. In `_fill_single_text_input()` - Detect numeric fields and use numeric fallback:
```python
# Before setting fallback, check if field expects number
field_type = inp.get_attribute("type") or "text"
if field_type == "number" or any(kw in question.lower() for kw in ["salary", "rate", "amount", "years", "months", "pkr", "usd", "compensation"]):
    inp.fill("0")
    logger.info(f"Fallback numeric: {question[:50]} = 0")
else:
    inp.fill(FALLBACK_TEXT)
    logger.info(f"Fallback text: {question[:50]}")
```

3. Add new patterns to answer_engine.py for these specific questions:
```python
# In checkbox_patterns or create new section
(r"sql.*postgres|postgres|mysql|sql\s*server", "multi_select", "sql_databases"),
(r"nosql|mongo|cassandra|dynamodb", "multi_select", "nosql_databases"),
(r"restful|rest\s*api|microservice", "dropdowns", "api_experience"),
(r"notice\s*period", "dropdowns", "notice_period"),
(r"python.*node|node.*python|years.*python", "dropdowns", "python_years"),
```

4. Add to config/answers.yaml:
```yaml
dropdowns:
  api_experience: "3-5 years"
  notice_period: "2 weeks"
  python_years: "5+"
  
multi_select:
  sql_databases: true
  nosql_databases: false
```

5. Create `_fill_multi_select_checkboxes()` method that specifically handles checkbox GROUPS:
- Find container elements with "Select all that apply" in question
- Check the first checkbox in the group as fallback
- For database questions, check "SQL" option

Files:
- C:\Projects\Mater-Browser\src\agent\linkedin_form_filler.py
- C:\Projects\Mater-Browser\src\agent\answer_engine.py
- C:\Projects\Mater-Browser\config\answers.yaml

The goal is: NEVER leave required fields empty. Always fill something.

## Code Standards

Write code that looks inevitable. Follow these constraints:

**Restraint**
- Solve it in one file if possible
- No abstractions until the third time you need them
- No classes if functions will do
- No inheritance - use composition

**Functions**
- Max 25 lines, aim for 15
- One level of nesting max
- Name describes exactly what it does: `extract_billable_hours()` not `process_data()`
- Input → transform → output. No side effects unless that's the point.

**Files**
- Max 200 lines for new files
- One clear responsibility
- If you're adding a second "system" to a file, stop and split

**No Ceremony**
- No AbstractFactory, no IServiceProvider, no Manager classes
- No code "just in case" - solve the actual problem
- Delete commented-out code, don't keep it

**Data**
- Use dataclasses or plain dicts, not classes with only __init__ and getters
- Data flows obviously - reader should predict what happens next
- No global state

The best code is code you delete. Every line is a liability.
