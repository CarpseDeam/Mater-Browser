## Task: Clean up Mater-Browser — Remove all bloat, LinkedIn Easy Apply only

**Project:** C:\Projects\Mater-Browser

## What to DELETE (files to remove entirely):

### Dead Claude-based form filling (replaced by deterministic filler):
1. `src/agent/claude.py` — old Claude API form analysis
2. `src/agent/prompts.py` — system prompts for Claude form filling
3. `src/agent/actions.py` — action models for Claude plans (FillAction, SelectAction, etc.)

### Dead Indeed flow (LinkedIn Easy Apply only):
4. `src/agent/external_flow.py` — Indeed/external ATS flow
5. `src/agent/indeed_form_filler.py` — Indeed form filling
6. `src/agent/indeed_helpers.py` — Indeed resume cards, modals

### Dead infrastructure (not wired in or superseded):
7. `src/agent/zero_actions_handler.py` — handled Claude 0-actions, dead since no Claude form filling
8. `src/agent/vision_fallback.py` — screenshot + Claude vision button finding, expensive and unreliable
9. `src/agent/navigation_helpers.py` — duplicate click_next, is_complete, try_resume_upload all reimplemented in linkedin_form_filler.py and linkedin_flow.py
10. `src/agent/loop_detector.py` — superseded by stuck_detection.py AND linkedin_flow's own hash-based detection
11. `src/agent/stuck_detection.py` — FormProcessorStuckDetection not wired into LinkedIn flow (linkedin_flow has its own stuck detection via _get_modal_hash)
12. `src/workflow/application.py` — entire old workflow using ClaudeAgent, completely superseded by linkedin_flow.py

### Dead feedback system (broken, never worked):
13. `src/feedback/auto_repairer.py` — sends wrong payload format to bridge, always fails silently
14. `src/feedback/config_suggester.py` — feeds auto_repairer, dead chain
15. `src/feedback/failure_summarizer.py` — feeds auto_repairer, dead chain

**NOTE:** KEEP `src/feedback/failure_logger.py` — it's used by answer_engine.py for logging unknown questions. That's useful data.

### Dead directories after cleanup:
16. `src/workflow/` directory — empty after removing application.py (delete the whole dir including __init__.py)

## What to MODIFY:

### `src/agent/application.py` — Simplify to LinkedIn-only
Current code routes to LinkedInFlow or ExternalFlow. Remove ExternalFlow routing entirely.
- Remove import of `ExternalFlow`
- Remove import of `INDEED_PATTERNS` from models
- Remove the Indeed/Direct detection in `_detect_source()`
- If source is not LinkedIn, return SKIPPED immediately
- Remove `JobSource.DICE` and `JobSource.DIRECT` usage

The simplified flow should be:
```python
def apply(self, job_url: str) -> ApplicationResult:
    if not self._is_linkedin(job_url):
        return ApplicationResult(status=ApplicationStatus.SKIPPED, message="Not LinkedIn", url=job_url)
    # ... LinkedIn flow only
```

### `src/agent/models.py` — Clean up dead references
- Remove `JobSource.DICE` and `JobSource.DIRECT` (keep LINKEDIN and INDEED for potential future)
- Remove `INDEED_PATTERNS` 
- Remove `ACCOUNT_CREATION_URL_PATTERNS` and `ACCOUNT_CREATION_CONTENT` (these are duplicated in page_classifier.py which has its own)
- Keep everything else (timeouts, ApplicationStatus, ApplicationResult, etc.)

### `src/agent/__init__.py` — Remove any exports of deleted modules

### `src/feedback/__init__.py` — Remove exports of deleted feedback modules (keep failure_logger)

### `src/agent/page_classifier.py` — Remove import of VisionFallback
- The PageClassifier does NOT import VisionFallback directly (ZeroActionsHandler does), but verify no references exist to deleted modules.

### `src/executor/runner.py` — Check if it imports anything from deleted modules (actions.py specifically)
- The ActionRunner likely imports from `actions.py`. If it does, AND it's only used by the deleted workflow, then `src/executor/runner.py` and the `src/executor/` directory can also be deleted.
- READ runner.py first to check before deciding.

### `src/extractor/dom_service.py` — Check if it's still used
- The DomService is used by the old ClaudeAgent workflow and old runner. If linkedin_form_filler.py and linkedin_flow.py don't use it, the entire `src/extractor/` directory might be dead too.
- READ the imports in linkedin_flow.py and linkedin_form_filler.py first — they use Playwright directly, not DomService.
- If nothing in the remaining code imports from `src/extractor/`, delete the entire `src/extractor/` directory.

## Code Standards:
- Static typing everywhere
- No bare `except:` 
- Use `logging` module
- Guard clauses, fail fast
- f-strings
- Keep functions under 25 lines where possible
- No unnecessary comments

## Process:
1. Read all imports across the surviving files to build a dependency graph
2. Delete files that are confirmed dead (no remaining imports)
3. Modify application.py and models.py
4. Clean up __init__.py files  
5. Delete empty directories
6. Verify the project still imports cleanly: `python -c "from src.agent.application import ApplicationAgent; print('OK')"`
7. Delete all `__pycache__` directories to clean up stale bytecode


## Code Standards

Write code that looks inevitable. Follow these constraints:

**Restraint**
- Solve it in one file if possible
- No abstractions until the third time you need them
- No classes if functions will do
- No inheritance - use composition

**Functions**
- Max 25 lines, aim for 15
- One level of nesting max
- Name describes exactly what it does: `extract_billable_hours()` not `process_data()`
- Input → transform → output. No side effects unless that's the point.

**Files**
- Max 200 lines for new files
- One clear responsibility
- If you're adding a second "system" to a file, stop and split

**No Ceremony**
- No AbstractFactory, no IServiceProvider, no Manager classes
- No code "just in case" - solve the actual problem
- Delete commented-out code, don't keep it

**Data**
- Use dataclasses or plain dicts, not classes with only __init__ and getters
- Data flows obviously - reader should predict what happens next
- No global state

The best code is code you delete. Every line is a liability.
