## Task: Fix Easy Apply button detection — add networkidle wait and fallback selectors

**Project:** C:\Projects\Mater-Browser
**File:** `src/agent/linkedin_flow.py`

## Problem
Easy Apply button not found because LinkedIn loads job details via XHR after domcontentloaded.

## Changes needed in apply() method:

1. After the `domcontentloaded` wait, add `networkidle` wait:
```python
try:
    self._page.raw.wait_for_load_state("networkidle", timeout=8000)
except Exception:
    pass
```

2. Replace single `#jobs-apply-button-id` locator/wait with call to new `_find_easy_apply_button()` method

3. Add new method:
```python
def _find_easy_apply_button(self) -> Optional[Locator]:
    """Find Easy Apply button using multiple selectors."""
    selectors_with_timeouts = [
        ('#jobs-apply-button-id', 5000),
        ('[data-live-test-job-apply-button]', 2000),
        ('button.jobs-apply-button', 2000),
        ('button[aria-label^="Easy Apply"]', 2000),
    ]
    for selector, timeout in selectors_with_timeouts:
        try:
            btn = self._page.raw.locator(selector).first
            btn.wait_for(state="visible", timeout=timeout)
            logger.info(f"Found Easy Apply button via: {selector}")
            return btn
        except Exception:
            continue
    return None
```

4. Import Locator at top: `from playwright.sync_api import Locator`

5. Update retry click section to use `self._find_easy_apply_button()` instead of hardcoded selector

## Verification
```
cd C:\Projects\Mater-Browser
python -c "from src.agent.linkedin_flow import LinkedInFlow; print('OK')"
```


## Code Standards

Write code that looks inevitable. Follow these constraints:

**Restraint**
- Solve it in one file if possible
- No abstractions until the third time you need them
- No classes if functions will do
- No inheritance - use composition

**Functions**
- Max 25 lines, aim for 15
- One level of nesting max
- Name describes exactly what it does: `extract_billable_hours()` not `process_data()`
- Input → transform → output. No side effects unless that's the point.

**Files**
- Max 200 lines for new files
- One clear responsibility
- If you're adding a second "system" to a file, stop and split

**No Ceremony**
- No AbstractFactory, no IServiceProvider, no Manager classes
- No code "just in case" - solve the actual problem
- Delete commented-out code, don't keep it

**Data**
- Use dataclasses or plain dicts, not classes with only __init__ and getters
- Data flows obviously - reader should predict what happens next
- No global state

The best code is code you delete. Every line is a liability.
