## Task: Analyze LinkedIn DOM dump and update selectors

**Project:** C:\Projects\Mater-Browser

## Step 1: Analyze the DOM dump

Read `C:\Projects\Mater-Browser\data\full_dom_dump.json` and extract:

1. **Every button** where text, aria-label, or className contains "apply" — print full details (text, aria, class, html preview, visibility)
2. **Every modal/dialog** — print class, role, aria-label, visibility  
3. **Every progress indicator** — print class, aria-valuenow, aria-valuemax
4. **Every button** that contains "next", "submit", "review", "continue", "dismiss", "close", "discard" in text or aria-label — print full details
5. **Every input, select, textarea** — print type, name, class, aria-label, placeholder, associated label text
6. **Every fieldset** with radio buttons — print legend, radio values, labels
7. **Every label** with class containing "form-element" or "fb-" — print text and class
8. **Every aria-label** on the page that contains "easy", "apply", "submit", "next", "review", "dismiss", "close"

Print all of this to stdout so we can see what LinkedIn actually uses.

## Step 2: Compare with current selectors

Read these files and compare what the DOM dump shows vs what the code uses:
- `src/agent/page_classifier.py` — LINKEDIN_EASY_APPLY_SELECTORS, _classify_apply_button logic, dismiss selectors
- `src/agent/linkedin_form_filler.py` — LinkedInSelectors class, SUBMIT_BUTTON_PATTERNS, click_next method, fill methods
- `src/agent/linkedin_flow.py` — _close_modal dismiss selectors, _get_modal_hash selectors, is_confirmation_page checks

For each selector in the code, report:
- MATCH: selector exists in the DOM dump
- MISS: selector does NOT exist in the DOM dump  
- NEW: selector exists in DOM but code doesn't use it

## Step 3: Update the code

Based on the analysis, update all three files with the correct selectors from the actual LinkedIn DOM. Key things to fix:

1. **page_classifier.py**: Update LINKEDIN_EASY_APPLY_SELECTORS to match what's actually on the page. Fix _classify_apply_button if the button text/aria doesn't contain "easy" (LinkedIn may have changed the button text format).

2. **linkedin_form_filler.py**: Update LinkedInSelectors class with actual CSS classes from the dump. Update SUBMIT_BUTTON_PATTERNS with actual button selectors. Update click_next to find buttons that actually exist.

3. **linkedin_flow.py**: Update _close_modal with dismiss button selectors that actually exist. Update _get_modal_hash with actual progress bar and form element selectors.

## Important
- The DOM dump is from a LIVE LinkedIn page as of today (March 2, 2026)
- LinkedIn changes their DOM frequently — use what the dump shows, not what you think it should be
- Keep fallback selectors for resilience but put the ones from the dump FIRST
- Don't remove old selectors entirely — just reorder so working ones come first

## Code Standards
- Static typing everywhere
- logging module
- Keep functions under 25 lines

## Verification
```
cd C:\Projects\Mater-Browser
python -c "from src.agent.linkedin_flow import LinkedInFlow; from src.agent.linkedin_form_filler import LinkedInFormFiller; from src.agent.page_classifier import PageClassifier; print('OK')"
```

